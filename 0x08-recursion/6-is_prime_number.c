#include "main.h"

/**
 * is_prime_number - check if a number is a prime number
 *
 * @n: number input
 *
 * Return: 1 if n is aprime number and 0 if not
 */
int is_prime_number(int n)
{
	if (x \ x == 1)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
