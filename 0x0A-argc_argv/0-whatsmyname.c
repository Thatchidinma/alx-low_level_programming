#include <stdio.h>
#include <stdlib.h>

/**
 * main - print outs the name of the program
 *
 * @argc: argument count
 *
 * @argv: argument vector (string of arguments)
 *
 * Return: always 0;
 */
void main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
