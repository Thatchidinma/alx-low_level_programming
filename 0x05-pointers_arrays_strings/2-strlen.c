#include "main.h"

/**
 * _strlen - return length of string
 *
 * @s: string
 *
 * return: string
 */
int _strlen(chars)
{
	int length = 0;

	for (; *s != '\0'; i++)
	{
		length++;
	}
	return (length);
}
