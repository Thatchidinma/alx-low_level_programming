#include <stdio.h>

/**
 * main - contains all block of code
 * for: satisfy conditions
 * Return: always return 0
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
